# Local development only. No patient data or production credentials.
POSTGRES_PASSWORD=local-demo-only
DATABASE_URL=postgres://optometry:local-demo-only@localhost:5432/optometry
PORT=4000
HOST=127.0.0.1
